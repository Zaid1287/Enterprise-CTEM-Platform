kind = "web"
previewPath = "/"
title = "CTEM Platform"
version = "1.0.0"
id = "artifacts/ctem-platform"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23203

[services.development]
run = "pnpm --filter @workspace/ctem-platform run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ctem-platform", "run", "build" ]
publicDir = "artifacts/ctem-platform/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23203"
BASE_PATH = "/"
